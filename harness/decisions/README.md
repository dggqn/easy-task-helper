# Architecture Decisions

重大技术、架构、依赖、数据结构和用户流程决策单独记录在本目录。

决策与普通开发日志的生命周期不同：日志可以在节点结束后压缩，决策需要
长期保留，以便解释当前架构为何如此形成。每项决策使用一个 Markdown 文件，
文件名采用 `YYYY-MM-DD-short-title.md`。

模板字段：Date、Status、Scope、Context、Options、Decision、Consequences。
